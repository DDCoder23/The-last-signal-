[package]
name = "the-last-signal-server"
version = "0.1.0"
edition = "2024"

[dependencies]
uuid = { version = "1", features = ["v4"] }
serde = { version = "1.0", features = ["derive"] }
rand = "0.8" 
serde_json = "1.0"
